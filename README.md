# 🔍 報名序號查詢系統

這是一個簡單的報名序號查詢網頁應用程式，使用者可以透過輸入 Email 來查詢其報名序號及相關資訊。

## ✨ 功能特色

- 📧 透過 Email 查詢報名序號
- 🔄 重新發送確認信功能
- 📱 響應式設計，支援手機與桌面裝置
- 🎨 現代化的使用者介面
- ⚡ 快速且即時的查詢回應

## 🚀 線上展示

> 請將此處替換為您的 GitHub Pages 網址
>
> 範例：`https://shoppingliao.github.io/tmoc-2026-jspoace-event-registration-lookup-app/`

## 📋 系統需求

- 現代網頁瀏覽器（Chrome、Firefox、Safari、Edge）
- 網際網路連線

## 🛠️ 技術架構

### 前端

- HTML5
- CSS3（含響應式設計）
- JavaScript (ES6+)
- Fetch API

### 後端

- Google Apps Script
- Google Sheets（作為資料庫）

## 📁 專案結構

```
tmoc-2026-jspoace-event-registration-lookup-app/
├── index.html          # 主要 HTML 頁面
├── css/
│   └── styles.css      # 樣式表
├── js/
│   └── main.js         # JavaScript 主程式
└── README.md           # 專案說明文件
```

## 🔧 安裝與設定

### 1. 克隆專案

```bash
git clone https://github.com/ShoppingLiao/tmoc-2026-jspoace-event-registration-lookup-app.git
cd tmoc-2026-jspoace-event-registration-lookup-app
```

### 2. 設定 Google Apps Script

1. 建立 Google Sheets 試算表
2. 建立 Google Apps Script 專案
3. 部署為 Web 應用程式
4. 複製 Web 應用程式 URL
5. 更新 `js/main.js` 中的 `API_URL`

```javascript
const API_URL = "YOUR_GOOGLE_APPS_SCRIPT_URL";
```

### 3. 本地測試

直接用瀏覽器開啟 `index.html` 檔案，或使用本地伺服器：

```bash
# 使用 Python 3
python3 -m http.server 8000

# 使用 Node.js (需先安裝 http-server)
npx http-server
```

然後在瀏覽器開啟 `http://localhost:8000`

## 🌐 部署到 GitHub Pages

### 方法一：透過 GitHub 網站設定

1. 將專案推送到 GitHub
2. 進入 Repository 的 Settings
3. 找到 Pages 選項
4. 在 Source 下選擇 `main` 分支
5. 點擊 Save
6. 等待幾分鐘後，您的網站就會上線

### 方法二：使用命令列

```bash
# 初始化 git（如果還沒初始化）
git init

# 加入所有檔案
git add .

# 提交變更
git commit -m "Initial commit"

# 連結遠端 repository
git remote add origin https://github.com/ShoppingLiao/tmoc-2026-jspoace-event-registration-lookup-app.git

# 推送到 GitHub
git push -u origin main
```

然後按照方法一的步驟 2-6 進行設定。

## 📝 使用說明

1. 開啟網頁
2. 在輸入框中輸入報名時使用的 Email
3. 點擊「查詢序號」按鈕
4. 系統會顯示您的報名序號及相關資訊
5. 如需重新發送確認信，可點擊「重新發送確認信」按鈕

## 🎨 客製化

### 修改顏色主題

編輯 `css/styles.css` 中的漸層顏色：

```css
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
```

### 修改 API 端點

編輯 `js/main.js` 中的 `API_URL` 常數。

## 🐛 常見問題

### Q: 查詢時出現 CORS 錯誤？

A: 請確認 Google Apps Script 已正確設定並重新部署，並將存取權限設為「任何人」。

### Q: 顯示 404 錯誤？

A: 請檢查 API_URL 是否正確，並確認 Google Apps Script 已成功部署。

### Q: 手機上顯示不正常？

A: 本專案已包含響應式設計，請清除瀏覽器快取後重試。

## 📄 授權

本專案採用 MIT 授權條款 - 詳見 [LICENSE](LICENSE) 檔案

## 👤 作者

**ShoppingLiao**

- GitHub: [@ShoppingLiao](https://github.com/ShoppingLiao)

## 🙏 致謝

感謝所有為這個專案提供建議和支援的人！

## 📮 聯絡方式

如有任何問題或建議，歡迎：

- 建立 Issue
- 發送 Pull Request
- 直接聯絡專案維護者

---

⭐ 如果這個專案對您有幫助，請給它一個星星！
